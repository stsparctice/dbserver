const routeEntityByItsType = async (entity, sqlCallback, mongoCallback) => {
    // if(mongo){
    //     convert condition to mongo
    // mongoCallback()
    // }
    //else{
    // convert condition to sql
    // sqlCallback
    // } 
}

module.exports = { routeEntityByItsType }